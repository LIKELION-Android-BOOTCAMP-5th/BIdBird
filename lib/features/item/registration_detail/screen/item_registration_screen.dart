import 'package:flutter/material.dart';

/// 이 파일은 더 이상 사용하지 않습니다.
/// 실제 ItemRegistrationScreen 은
/// features/item/registration/screen/item_registration_screen.dart 에 정의되어 있습니다.
class ItemRegistrationScreen extends StatelessWidget {
  const ItemRegistrationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'ItemRegistrationScreen is defined in features/item/registration/screen',
          textAlign: TextAlign.center,
        ),
      ),
    );
  }
}
